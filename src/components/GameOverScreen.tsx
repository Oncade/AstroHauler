import React from 'react';

// This component is intentionally empty as the Phaser scene handles all the UI
// to prevent duplication issues
export const GameOverScreen: React.FC = () => {
    console.log('GameOverScreen React component rendered (EMPTY)');
    return null;
}; 