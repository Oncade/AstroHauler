import React from 'react';

// Placeholder for the Main Menu React Component
// This component might display high scores, options, etc., fetched via EventBus or context

export const MainMenu: React.FC = () => {
    // Using an empty React component as the Phaser scene is handling the UI
    // This prevents UI duplication issues
    console.log('MainMenu React component rendered (EMPTY)');
    return null;
}; 